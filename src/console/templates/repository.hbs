import { EntityRepository, Repository } from 'typeorm';

import { {{name.capitalize}} } from '../models/{{name.capitalize}}';

@EntityRepository({{name.capitalize}})
export class {{name.capitalize}}Repository extends Repository<{{name.capitalize}}>  {

}
